# =============================================================================
# Code Intelligence Platform - Python Dependencies
# =============================================================================

# --- Core Framework ---
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.5.0

# --- HTTP & Utilities ---
requests>=2.31.0
python-dotenv>=1.0.0
boto3>=1.34.0
botocore>=1.34.0
langsmith>=0.7.0,<0.8.0
langchain>=0.3.0
langchain-core>=0.3.0
langchain-openai>=0.2.0

# --- Security Scanning (Python) ---
bandit>=1.7.0
pip-licenses>=4.3.0

# --- RAG / Vector Database ---
sentence-transformers>=2.2.0
faiss-cpu>=1.7.4
qdrant-client>=1.7.0

# --- LLM Providers (install as needed) ---
# For OpenAI (production):
# openai>=1.6.0
# For Ollama (development - default, no pip package needed)
# For Hugging Face (optional):
# transformers>=4.36.0
# torch>=2.1.0

# --- Java Scanning Tools (install separately, not via pip) ---
# SpotBugs:  brew install spotbugs  (or download from GitHub)
# OWASP Dependency-Check:  brew install dependency-check

# --- Testing (excluded from production image — see Dockerfile deps-prod stage) ---
pytest>=7.4.0
pytest-asyncio>=0.21.0
httpx>=0.25.0
hypothesis>=6.88.0
